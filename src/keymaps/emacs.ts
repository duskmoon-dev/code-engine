// [DUSKMOON] Emacs keymap — placeholder for @replit/codemirror-emacs fork
// TODO: Fork and inline @replit/codemirror-emacs
import type {Extension} from "../core/state/index"

/// Emacs keybindings for CodeMirror.
/// This is a placeholder — the full implementation will be forked from @replit/codemirror-emacs.
export function emacs(): Extension {
  return []
}
