// [DUSKMOON] Vim keymap — placeholder for @replit/codemirror-vim fork
// TODO: Fork and inline @replit/codemirror-vim
import type {Extension} from "../core/state/index"

/// Vim keybindings for CodeMirror.
/// This is a placeholder — the full implementation will be forked from @replit/codemirror-vim.
export function vim(): Extension {
  return []
}
